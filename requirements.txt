# ============================================================
# requirements.txt
# ============================================================
# Pinning strategy: ">=" minimum + "<" next major to get security
# patches automatically while blocking breaking API changes.
# ============================================================

# ── FastAPI core ──────────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0         # ASGI server (includes websockets, httptools)
python-multipart>=0.0.9,<1.0.0            # Required for form data / file uploads

# ── Pydantic (data validation & settings) ────────────────────────────────
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0          # BaseSettings (.env support)

# ── Authentication ────────────────────────────────────────────────────────
PyJWT>=2.8.0,<3.0.0                       # JWT encode/decode
bcrypt>=4.0.0,<5.0.0                      # Password hashing (bcrypt direct)

# ── Async HTTP client (replaces blocking requests in API layer) ───────────
httpx>=0.27.0,<1.0.0

# ── Environment variables ─────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0

# ── Bot runtime dependencies (existing bots use these) ───────────────────
requests>=2.32.0,<3.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<3.0.0
pytz>=2024.1
websocket-client>=1.9.0,<2.0.0
